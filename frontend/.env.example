# Optional. Only needed if your backend isn't running on the default
# http://localhost:3005 (e.g. you deployed it somewhere else).
# Copy this file to .env and uncomment the line below to override.

# VITE_BACKEND_URL=http://localhost:3005
